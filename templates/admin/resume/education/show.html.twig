{% extends '../../../base.html.twig' %}

{% block title %}Education{% endblock %}

{% block body %}
    <h1>Education</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ education.start ? education.start|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ education.end ? education.end|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>School</th>
                <td>{{ education.school }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ education.city }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ education.description }}</td>
            </tr>
            <tr>
                <th>Diploma</th>
                <td>{{ education.diploma }}</td>
            </tr>
            <tr>
                <th>Specality</th>
                <td>{{ education.specality }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_index') }}">back to list</a>

    <a href="{{ path('app_education_edit', {'id': education.id}) }}">edit</a>

    {{ include('education/_delete_form.html.twig') }}
{% endblock %}
